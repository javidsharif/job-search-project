spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/job-search
    username: postgres
#    password: 12345
    password: bestuser
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    locations: "classpath:db.migration"

    schemas: public
    baseline-on-migrate: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  cloud:
    gcp:
      projectId: "job-search-project-395115"
      credentials:
        location: "file:C:\\Users\\99451\\Downloads\\job-search-project-395115-3f2f481bfdeb.json"
  servlet:
    multipart:
      max-file-size: 10MB
gcp:
  bucket:
    name: "job-search-bucket"